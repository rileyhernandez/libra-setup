[Unit]
Description=Libra Process
After=libra-update.service
Wants=libra-update.service

[Service]
ExecStart=/usr/local/bin/libra-inventory
Type=simple
User=libra
Group=libra
Restart=always
RestartSec=3
ProtectSystem=full
ReadWritePaths=/var/lib/libra

[Install]
WantedBy=multi-user.target